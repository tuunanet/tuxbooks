# Instructions for agents

This directory holds the freely licensed ebook fixture corpus, downloaded by
`just fetch-ebooks` (scripts/fetch-ebook-fixtures.py) and verified against the
committed `manifest.json` (`just check-ebooks`). See `docs/free-ebook-fixtures.md`.
Only `manifest.json` is committed; the book files themselves are not.

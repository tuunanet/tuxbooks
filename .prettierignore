node_modules
dist
dist-packages
sidecar/target
pnpm-lock.yaml
e2e/.tmp
.superpowers
*.png
*.ico
*.icns
LICENSE.md

# EPUB fixture corpus: source trees + READMEs are generated/script-owned and
# must stay byte-stable for the determinism check (just make-epub-fixtures)
tests/fixtures/epub

# Fuzz seeds (issue #88): libFuzzer corpus units, one input per file — not
# app code, and byte-stable seeds keep fuzz runs comparable
sidecar/fuzz

# Vendored agent skills: installed from upstream and hash-locked in
# skills-lock.json. Reformatting them desyncs the lock and diverges from
# upstream, so keep the checked-in copies byte-stable. See AGENTS.md.
.agents/skills

# Beads state (written by bd): tracked so teammates can read the tracker, but
# the bytes belong to bd. bd rewrites them in its own style, and prettier does
# not read the nested .beads/.gitignore that keeps runtime files out of git.
.beads/

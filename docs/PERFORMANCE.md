# Performance budgets and metrics

The readers are budgeted in **pixels and bytes, not element counts**. Every
surface they produce scales with viewport area, and the app must stay smooth
at a maximized 3840×2160 window — not just at the default 1100×720 size.

Like the coverage gate (`docs/COVERAGE.md`), budgets are changed only case
by case, with the reason recorded in the same change. Unlike coverage, not
every budget is machine-enforced yet; the Verified-by column states what
holds each one today, and what must hold it in a change that touches it.

## Reference conditions

All numeric budgets are evaluated at: maximized window on 3840×2160,
devicePixelRatio 1.0 **and** 2.0, letter PDF page (612×792 pt) at fit-width
zoom 100%, EPUB default (paginated) flow, current Electron/Chromium.
A change that makes any budget worse at reference conditions must state the
measured delta in the PR description.

Chromium does not automatically guarantee good performance — budget
regressions under Electron are re-measured, not assumed away. Do not
treat Chromium's compositing as a substitute for the pixel/byte budgets.

## Budgets

| ID      | Metric                                                      | Required                                                                                                                                                                                                                                                                  | Status (2026-09-21)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Verified by                                                                                         |
| ------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| PERF-1  | PDF page canvas backing store (`displayed px × dpr²`)       | ≤ 2²⁵ px total **and** ≤ 8192 px per dimension; CSS upscales beyond that                                                                                                                                                                                                  | Two-tier cap (`pdfRenderPolicy`): prefers dpr → degrades to the soft tier → CSS-resolution floor → hard cap under zoom. Carry over unchanged through the PDF engine migration                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Unit test pinning the cap math (`pdfRenderPolicy.test.ts`, jsdom `devicePixelRatio`)                |
| PERF-2  | Visible-page raster latency (render start → blit)           | p95 ≤ 100 ms at reference conditions                                                                                                                                                                                                                                      | Pre-PDFium baseline (2026-09-09, phase 6): p50 147.5 ms / p95 176.5 ms, maximized 2643×1405 at dpr 1.45 (closest achievable here; bench has no dpr override). The engine worker raster dominates at this viewport; exceeds the WebKitGTK-era threshold — enforce at true reference conditions (3840×2160, dpr 1/2) before treating 100 ms as a gate. Post-PDFium measurement (2026-09-21, `tuxbooks-koe.15`, `just bench-reader`): p50 148.9 ms / p95 209.1 ms (n=17) in one run and p50 172.4 ms / p95 243.5 ms in another, same maximized 2643×1405 dpr-1.453 window (the bench has no dpr override; this 4K host runs the read surface at dpr 1.45, so it cannot produce the dpr-2 maximum backing store). PDFium matches the MuPDF p50 and is within run-to-run variance on p95; both engines miss 100 ms here because the worker raster dominates. The Phase 0 spike's dpr-2 result stands as the re-baseline item: 100 ms p95 is met at dpr 1 but missed at the largest dpr-2 region size on image-heavy pages, mirroring the MuPDF baseline. Treat this as a re-baseline, not a failure                          | Manual measurement (`just bench-reader`); diagnostic attrs                                          |
| PERF-3  | Bitmap cache occupancy after one down-up scroll oscillation | ≥ 2 buffers retained; cache byte budget ≥ 2 × largest page buffer                                                                                                                                                                                                         | 320 MB budget ≥ 2 × capped 4K buffer at both reference dprs; unit tests pin both                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `data-pdf-bitmap-cache` attr + unit test on budget math                                             |
| PERF-4  | Total bytes of mounted page canvases                        | ≤ 256 MB at reference conditions (byte budget governs; count cap stays ≤ 8)                                                                                                                                                                                               | Byte-budgeted render window (`MAX_ACTIVE_CANVAS_BYTES`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Derived from slot/canvas state in unit tests                                                        |
| PERF-5  | React state commits from scrolling                          | ≤ 2 per 1000 px of continuous scroll (page/section changes only); scroll handlers rAF-coalesced                                                                                                                                                                           | Do not regress                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Vitest with synthetic scroll events                                                                 |
| PERF-6  | Compositing hygiene                                         | No `box-shadow`/`filter`/`backdrop-filter`/`blur` on page-sized or larger layers (canvases, EPUB iframes); decorations live on cheap wrappers                                                                                                                             | Review + `grep` over reader components                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Review + `grep` over reader components                                                              |
| PERF-7  | EPUB relocation debounce                                    | ≥ 200 ms in continuous/scrolled flow                                                                                                                                                                                                                                      | Holds: the app never overrides the engine's relocate cadence; saves debounce 1 s (`useReaderProgress`) and position reporting is event-driven, never polled                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Code review when touching navigator settings                                                        |
| PERF-8  | EPUB paginated per-turn raster volume                       | ≤ 1 viewport area per page turn (one column/viewport)                                                                                                                                                                                                                     | Re-establish with Readium at phase 3 (Readium CSS pagination)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Review when touching navigator/pagination settings                                                  |
| PERF-9  | Reading-progress write discipline                           | Saves debounced ≥ 1 s; first armed save skipped (opening writes nothing)                                                                                                                                                                                                  | `useReaderProgress` contract — engine-agnostic, survives the migration                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Existing `useReaderProgress` unit tests                                                             |
| PERF-10 | Book payload transport                                      | Raw bytes via `tuxbooks://` (range requests), never JSON/base64-encoded growth                                                                                                                                                                                            | New protocol replaces the old raw-IPC path; verify no base64 inflation over the bridge                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | E2E + review of the protocol handler + sidecar reader service                                       |
| PERF-11 | Renderer hygiene                                            | No GPU-workaround env flags in shipped code (`ELECTRON_*`/`--disable-gpu` without cause); startup logs dpr, content area, fit scale                                                                                                                                       | Chromium replaces the WebKitGTK dmabuf/compositing-flag minefield; any Electron `app.commandLine` GPU flags need a written reason here. Sanctioned exception: the crash-driven software-rendering fallback (`docs/gpu-fallback.md`) sets `--disable-gpu` via `app.disableHardwareAcceleration()` only when the previous session recorded repeated GPU-process crashes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `grep` + review + `data-pdf-render-info` attr                                                       |
| PERF-12 | EPUB scrolled reading-surface width                         | ≤ 960 px inline (`EPUB_SCROLLED_SURFACE_MAX_PX`), centered; paginated flow stays uncapped — the engine's pagination bounds its spread                                                                                                                                     | Holds: the seam constant caps the scrolled surface (`readiumEngine.ts`), pinned by unit tests + bench measure attribute                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Unit tests pinning the cap + bench asserts the measure attribute                                    |
| PERF-13 | Process overhead                                            | Sidecar is spawned once and reused; no per-turn IPC for operations that can stay inside the renderer (scroll, zoom, render); no chatty poll loops. Parse/extraction ops pay one fork and exec into the per-job document worker (ADR 0001); reader byte-range paths do not | New budget for the Electron stack — the bridge is for data/state, never per-frame traffic; measure with `just bench-reader` before and after any worker transport change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Review + bridge call-count assertions in unit tests where practical                                 |
| PERF-14 | PDF open path — first useful page                           | The first readable page must not wait for the whole file: documents open through the range-backed `FPDF_LoadCustomDocument` + `FPDF_FILEACCESS` (`openPdfDocumentFromBook`); engine prewarm skips WASM cold start; outline/text/thumbnails are ordered below first paint  | Range-backed open + engine prewarm landed (2026-09-09); click→open / firstPaint / interactive segments recorded on the reader element. Post-PDFium measurement (2026-09-21, `tuxbooks-koe.15`): the seeded 100-page fixture reports `bytes=range;open=16;firstPaint=238;interactive=238` (open 16–17 ms, firstPaint 238–244 ms across dpr-1 and dpr-1.45 runs; `interactive` equals first paint on a fresh position). Timing thresholds are manual-bench values, never CI gates                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `data-pdf-open-state` / `data-pdf-open-timing` attrs (E2E asserts state only) + `just bench-reader` |
| PERF-15 | Rendered library cards (issue #61)                          | The library grid/list renders a bounded window (visible rows + overscan), never one node per book — DOM card count stays constant as the library grows                                                                                                                    | Virtualized grid + list (`@tanstack/react-virtual`, `LibraryView`); no-geometry environments (jsdom/SSR) take the bounded static fallback                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Unit test with 2,000 books asserting the window cap (`libraryScale.test.tsx`)                       |
| PERF-16 | Library event → commit ratio (issue #61)                    | Backend book events (`import-progress`, `library-changed`) are coalesced: buffered changes land as at most one commit per `EVENT_FLUSH_MS` window regardless of event count                                                                                               | Buffered flush in `useLibraryData` (2026-09-14); list patching is O(1) per change (id→index map), inserts append and the completing refresh reconciles order                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Unit test: 50 events produce ≤ 6 renders (`libraryScale.test.tsx`)                                  |
| PERF-17 | PDF deep-zoom raster policy                                 | Above the whole-page budget, rasterize only the visible region at device resolution — no page-sized buffer, no CSS upscale of sub-display-resolution pixels                                                                                                               | Viewport-clipped region rendering: `needsRegionRender`/`regionRenderRatio` (`pdfRenderPolicy`), `visiblePageRegion` (`pdfLayout`), a region-sized, absolutely positioned canvas in `PdfPageCanvas`, and a clip rendered through `FPDF_RenderPageBitmapWithMatrix` in `pdfiumCore`. Pre-PDFium baseline (100-page fixture, Smart dark, 10000%): largest page buffer 33.5 MP → 3.6 MP, max main-thread frame gap 283 ms → 50 ms. Fit-width path unchanged. Superseding rasters coalesce (150 ms settle, first paint immediate) so rapid zoom cannot queue worker renders. PDFium re-baselined at deep zoom (`tuxbooks-koe.15`, 2026-09-21): the image-heavy GeoTopo page 35 reports a region canvas (`data-pdf-render-region` non-`full`, 2048×2560 CSS / 2976×3720 backing ≈ 11.1 MP at dpr 1.45) with `data-pdf-render-ms` samples of 88.9–182.0 ms, and the bench's fit-width drag render→blit stays in the same band; the dpr-2 7680×4320 region case from the Phase 0 spike remains the PERF-2 re-baseline item. Content culling beyond that (skipping off-clip draw execution) remains open if profiling demands it | Unit (region math, policy, cache key) + E2E `data-pdf-render-region` geometry                       |

## Rules for changes

- Any change touching the touch-list below must re-read this doc and keep
  every budget at or better than required — the fix for a violation is a
  fix, not a raised threshold.
- Growing per-page raster pixels, per-frame composited area, or per-scroll
  main-thread work requires the measured delta at reference conditions in
  the PR description and updated pinning tests in the same change.
- New diagnostics must be deterministic DOM attributes (`data-*`), never
  timing assertions — E2E runs headless under `xvfb-run`, where timings are
  unreliable. Timing budgets (PERF-2) are measured manually or via logs,
  not asserted in CI.

## Touch-list

Re-run the relevant verifications when editing:

- `PdfPageCanvas` / `pdfBitmapCache` / `usePdfVirtualization` /
  `usePdfViewport` / `PdfReader` render-policy constants
  (`MAX_ACTIVE_CANVASES`, `MAX_CONCURRENT_RENDERS`, `REGION_OVERSCAN_PX`)
  and the region helpers in `pdfLayout`/`pdfRenderPolicy` — PERF-1 to
  PERF-6, PERF-17.
- `usePdfScrollTracking`, `ReaderShell` scroll container — PERF-5, PERF-9.
- EPUB engine seam (Readium navigator options, flow/appearance wiring,
  scrolled-surface cap) — PERF-7, PERF-8, PERF-12.
- `useReaderProgress`, reader persistence — PERF-9.
- `tuxbooks://` protocol handler + sidecar `reader` service — PERF-10.
- Electron `app.commandLine` / GPU flags — PERF-11.
- Bridge call sites (renderer↔main↔sidecar) — PERF-13.
- `LibraryView` (virtualizer options, column metrics, keyboard focus
  model), `BookCover`, `useLibraryData` batching/patching — PERF-15,
  PERF-16.
- Scale corpus + spec (`scripts/make-epub-corpus.py`,
  `e2e/specs/z-library-scale.e2e.ts`: window cap, sort liveness, skip
  pass) — PERF-15, PERF-16.

## How to measure

- **Unit (deterministic):** mock `devicePixelRatio` in jsdom and pin the
  cap/budget math (PERF-1, PERF-3, PERF-4) and scroll-commit counts
  (PERF-5, synthetic scroll events).
- **E2E (deterministic attrs only):** assert on `data-pdf-bitmap-cache`
  (`entries:bytes`) and `data-render-state` churn — e.g. cache occupancy
  after a scripted scroll oscillation.
- **Bench suite (headed, explicit):** `just bench-reader [WxH]` runs the
  `bench-reader.e2e.ts` suite on the real display, window maximized
  (renderer-side `window.resizeTo` sizes the real OS window the reader
  lays out against). It collects: PDF
  page-walk `data-pdf-render-ms` (PERF-1/3/4 budget checks asserted),
  synthetic scrollbar drags on both readers with a rAF frame-interval
  sampler (p50/p95/max frame time and the dropped-frame share over
  16.7/32/50 ms), interaction latency for the reader's real interaction
  patterns (PDF rapid page turns, zoom steps, Ctrl+wheel gesture settle
  (last event → sharp raster), large-document scroll jumps;
  EPUB chapter changes), first-render latency (launch → first rendered
  page), and live canvas memory (PERF-4). The PDF-open timeline
  (PERF-14) extends first-render measurement: the reader element carries
  `data-pdf-open-timing` (`bytes=range;open=…;firstPaint=…;interactive=…`),
  so click→document-open and click→first-useful-page are read from the same
  bench run (cold vs. prewarmed engine shows up as the `open=` delta).
  Results land in
  `artifacts/e2e/<runId>/bench-results.json` plus a one-line summary in
  `bench-trend.jsonl` for run-over-run drift comparison. Never runs in CI —
  timings are unreliable headless and the suite is the manual-measurement
  tool for PERF-2. Timing thresholds are opt-in via `BENCH_ENFORCE_P95_MS`,
  and a threshold that fires on machine variance is compared against the
  trend file before being changed. The high-DPI reference condition has its
  own deterministic configuration: `just test-e2e-hidpi` forces
  `devicePixelRatio` 2 and asserts the buffer caps (PERF-1) at that dpr
  (timing stays out of it).
- **Manual (timings):** log `performance.now()` around render → blit per
  page at reference conditions; record p95 before/after engine, scale, or
  virtualization changes. Chromium DevTools performance panel and
  `chrome://tracing` replace the WebKitGTK-era Sysprof workflow.

## Legacy notes (WebKitGTK era, kept for context)

The old stack capped the webview frame clock at ~31 fps idle on Wayland
(reproduced in bare GTK hosts; root cause WebKit bug 315997) — PERF-2
p95 measurements from before the migration reflect that environment, not
the app. Historical analysis: `docs/research/webview-frame-clock.md`,
`docs/performance-4k.md`, `docs/research/reader-perf-bench-handover.md`.
Re-baseline everything on Chromium in migration phase 6; do not carry
WebKitGTK-specific tuning decisions forward.
